$("#complete").on("click", function() {
  let gunCount = 0;
  let bullCount = 0;
  let parCount = 0;
  let initDCount = 0;
  let dartCount = 0;
  let zeldaCount = 0;
  let strageCount = 0;
  let coffeeCount = 0;
  let guacCount = 0;
  let hamCount = 0;

  parseInt($("#gun").val());
  parseInt($("#bull").val());
  parseInt($("#par").val());
  parseInt($("#initD").val());
  parseInt($("#darts").val());
  parseInt($("#zelda").val());
  parseInt($("#strange").val());
  parseInt($("#coffee").val());
  parseInt($("#guac").val());
  parseInt($("#ham").val());
});
